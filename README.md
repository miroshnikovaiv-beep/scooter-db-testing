
---

## 🧪 Работа с базой данных

### Задание 1  
Вывести список логинов курьеров и количество их заказов  
в статусе **«В доставке»** (`inDelivery = true`).

Решение реализовано SQL-запросом:
- `sql/task_1_orders_in_delivery.sql`

Результат выполнения:
- `results/task_1_result.md`

---

### Задание 2  
Проверка корректности статусов заказов.

Правила определения статуса:
- `finished = true` → статус **2**
- `cancelled = true` → статус **-1**
- `inDelivery = true` → статус **1**
- остальные случаи → **0**

Решение:
- `sql/task_2_order_statuses.sql`

---

## 🤖 Автоматизация API-теста

Реализован автотест, который проверяет сценарий:

1. Клиент создаёт заказ (POST)
2. Сохраняется номер трека заказа
3. Получение заказа по треку (GET)
4. Проверка, что сервер возвращает **HTTP 200**

### Используемые технологии
- Python 3
- pytest
- requests

---

## ▶️ Запуск автотестов

1. Создать виртуальное окружение:
```bash
python -m venv .venv
