## Задание 1. Заказы в статусе «В доставке»

Результат выполнения запроса:

- Заказы со статусом `inDelivery = true` отсутствуют
- Количество строк в результате: 0

Дополнительные проверки:
- COUNT(*) WHERE inDelivery = true → 0
- COUNT(*) WHERE courierId IS NOT NULL → 0

Вывод:
На момент проверки в базе отсутствуют заказы в статусе «В доставке».
